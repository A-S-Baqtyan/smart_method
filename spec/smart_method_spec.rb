# frozen_string_literal: true

RSpec.describe SmartMethod do
  it "has a version number" do
    expect(SmartMethod::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
